<div class="flex justify-center">
  <div class="flex flex-col mt-20 mb-4">
    <% if @skills.present? %>
      <div class="mt-8 grid grid-cols-2">
        <%= render @skills %>
      </div>
    <% end %>
  </div>
</div>